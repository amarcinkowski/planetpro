package io.github.amarcinkowski;

public class ObliczPole {

	
	/*public static double getPolePrzekroju(SpaceObject object) {
		return Math.pow(object.getPromien(),2) * Math.PI;
	}
	
	public static double getPolePowierzchniObiektu(SpaceObject object) {
		return Math.pow(object.getPromien(),2) * Math.PI * 4;
	}
	
	public static double getPolePowierzchniSfery(SpaceObject object) {
		return Math.pow(object.getOdlegloscOdSlonca(),2) * Math.PI * 4;
	}
	
	public static double getProcentEklipsa(SpaceObject object) {
		return getPolePrzekroju(object) / getPolePowierzchniSfery(object);
	}
	
	public static void main(String[] args) {
		System.out.println(getProcentEklipsa(SolarSystemBodies.get("Ziemia")));
	}*/
}
